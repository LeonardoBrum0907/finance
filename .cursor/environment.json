{
  "name": "finance",
  "install": "pnpm install && pnpm db:generate",
  "start": "bash scripts/sync-api-env-from-cloud.sh && bash scripts/ensure-db.sh",
  "terminals": [
    {
      "name": "dev",
      "command": "bash scripts/sync-api-env-from-cloud.sh && pnpm exec prisma migrate deploy --schema apps/api/prisma/schema.prisma && pnpm dev",
      "description": "API (3333) + Web (5173)"
    }
  ],
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "api", "port": 3333 }
  ]
}
